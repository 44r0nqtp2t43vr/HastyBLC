@model BookListViewModel
@section styles {
    <link rel="stylesheet" href="~/css/HastyBLCStyle.css" />
}

@{
    ViewData["Title"] = "Book Page";
}

<html>
<body>
    <div class="sidebar">
        <div class="image">
            <img src="~/img/logo.png" class="imagehw">
        </div>
        <div class="hasty">
            <h1>HASTY</h1>
            <h1>BLC</h1>
        </div>
        <div class="screens">
            <a asp-controller="Dashboard" asp-action="Dashboard">Dashboard</a>
            <a asp-controller="Genres" asp-action="Genres">Genres</a>
            <a asp-controller="Books" asp-action="Books" class="active">Books</a>
        </div>
    </div>
    <div class="content">
        <div class="all-books-label">ALL BOOKS</div>
            <div class="add-book-btn-container">
                <a asp-controller="Books" asp-action="AddBook" class="btn btn-outline-danger">Add Book</a>
            </div>
           
            <div class="books-row">
                @foreach (var book in Model.Books!)
                {
                    <div class="book-item">
                        <div class="book-image-container">
                       
                        <img src="~/img/@book.Image" alt="@book.Title" class="book-image" />
                        </div>
                        <div class="book-details-text">
                        <h2>@book.Title</h2>
                       <!---- <ul>
                            <li>@book.AuthorName</li>
                            <li>@book.PublishDate</li>
                             <li>
                                @{
                                    if (book.Genres != null)
                                    {
                                        var uniqueGenres = book.Genres.Distinct();
                                        <text>@string.Join(", ", uniqueGenres)</text>
                                    }
                                    else
                                    {
                                        <text>No Genres</text>
                                    }
                                }
                            </li>
                        </ul>-->
                        </div>
               <!---- <div class="d-flex align-items-center justify-content-center pb-4">
                    <a asp-controller="Books" asp-action="EditBook" asp-route-isbn="@book.Isbn" class="btn btn-outline-danger">Edit Book</a>
                </div>-->
               <!---- <form method="post" asp-action="DeleteBook" class="li3">
                    <input type="hidden" name="bookId" value="@book.BookId" />
                    <button type="submit" class="btn btn-outline-danger">Delete</button>
                </form>-->
                </div>
            }

        </div>
    </div>
   
</body>
</html>
